const myString = 'developer';

const firstChar = myString.charAt(0).toUpperCase();
console.log(firstChar); // 'D'

const restofString = myString.slice(1);
console.log(restofString); // 

console.log(firstChar + restofString);
